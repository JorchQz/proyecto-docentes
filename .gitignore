# Material de referencia (binarios pesados: PDFs, xlsx) — solo en disco local
docs/referencia/

# Configuración local de Claude Code (no compartir)
.claude/settings.local.json

# Archivos del sistema operativo
Thumbs.db
.DS_Store

# Logs y temporales
*.log
*.tmp

# Estado local de la CLI de Supabase
supabase/.temp/

# Secretos locales (tokens de Supabase y Mercado Pago) — NUNCA subir.
# Se ignora todo .env*; solo la plantilla vacía queda versionada.
.env*
!.env.ejemplo
